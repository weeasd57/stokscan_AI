# تقرير اختبار أسئلة المستخدمين

تاريخ الاختبار: 2026-08-07

تم تشغيل 15 سؤالاً من الحسابات الفعلية عبر السيرفر المحلي `http://127.0.0.1:3000` باستخدام جلسة كل مستخدم، وجميع الطلبات عادت بحالة HTTP 200.

## weeessd57@gmail.com

السؤال: `الاسكندرية والمطاحن والشمس - اشتري مين بكره؟`

النتيجة:

- تم استخراج الأسهم: `ALCN`, `AFMC`, `ELSH`.
- `ALCN`: السعر 30.70، التغير -1.54%، RSI 64.97، الحجم 1.05x.
- `AFMC`: السعر 224، التغير -3.44%، RSI 76.66، الحجم 0.80x.
- `ELSH`: السعر 14، التغير -1.06%، RSI 45.25، الحجم 0.73x.
- الرد لم يحدد السهم الأفضل، واكتفى بعبارة عدم القدرة على اتخاذ القرار وقراءة عامة.

الحكم: فشل وظيفي. السؤال يطلب مقارنة مباشرة واختيار الأفضل، والرد لم يقدّم ترتيباً أو اختياراً.

## b.ahmed2113@gmail.com

### السؤال عن أسهم التجميع

النتيجة الأولى عرضت 15 سهماً من مسح بتاريخ `2026-07-27`، وكان من أبرزها `AJWA`, `CPME`, `ASPI`, `DGTZ`, `AFMC`. البيانات نفسها مفيدة، لكن الرد لم يشرح أن المسح أقدم من أحدث بيانات الأسعار، ولم يحدد أفضل سهم رغم طلب فرص الصعود القريبة.

الحكم: جزئي. البيانات صحيحة كمسح، لكن الترتيب والتوصية العملية غير مكتملين.

### سؤال منطقة سهم جدوى

النتيجة: `GDWA` بسعر 0.816، RSI 38.24، حجم 0.53x، دعم 0.76 ومقاومة 0.89. الرد وصفه بأنه ضعيف إلى محايد، لكنه لم يصنفه صراحة ضمن منطقة دعم أو حياد أو مقاومة كما طلب المستخدم.

الحكم: جزئي. المؤشرات موجودة، لكن تصنيف المنطقة غير مباشر.

### المسح الفني المركب لسهم CPME

النتيجة تضمنت بيانات متعارضة:

- جدول السعر الحالي: RSI 78.37، الحجم 0.22x، التغير -4.73%.
- جدول مسح التجميع: RSI 64.21، الحجم 2.516x، التغير +3.1198%، التاريخ 2026-07-27.
- الرد استخدم RSI 78.37 ووصف MACD بأنه صاعد، لكنه لم يعرض المتوسطات المتحركة المطلوبة.

الحكم: فشل وظيفي عالي الخطورة. يجب فصل تاريخ المؤشرات بوضوح وعدم دمج بيانات حديثة وقديمة في حكم واحد.

### سؤال CPME: تجميع أم تصريف

النتيجة: تجميع قوي بدرجة 80.3، وثلاثة أيام تجميع، بتاريخ 2026-07-27.

الحكم: ناجح، مع ملاحظة أن التاريخ القديم يجب أن يظهر دائماً في الرد.

### شروط التجميع المرسلة نصياً

النتيجة الأولى أعادت `CPME` فقط بدرجة 80.3، رغم أن المسح السابق يحتوي على عدة أسهم تحقق جزءاً من الشروط. النتيجة الثانية أعادت قائمة شروط صحيحة شكلياً، لكن الرد قال إن `CPME` وحده يطابقها ولم يذكر سبب استبعاد باقي الأسهم أو يطبق شرط يومين بوضوح.

الحكم: فشل وظيفي. العبارة النصية يجب أن تتحول إلى فلتر بيانات صريح: `acc_score > 75`, `vol_ratio > 1.5`, `dist_score = 0`, و`consecutive_acc_days >= 2`.

## abdallahsaied912@gmail.com

### BIOC

السؤال `Bioc مشترية عند 383` لم يحفظ الرمز أو البيانات في الرد، ثم سؤال `مستهدفاته ايه` أعاد RSI عاماً بلا مستويات أو هدف. عند كتابة `مستهدفاته ايه bioc ?` ظهرت بيانات BIOC، لكن الرد رفض اعتبار المقاومة 497.16 هدفاً، ولم يقدم سيناريوهات دعم/مقاومة عملية.

الحكم: جزئي إلى فاشل. المتابعة القصيرة فقدت مرجع السهم، وسؤال الهدف لم يعرض مستويات قابلة للاستخدام.

### CRST

السؤال `Crst?` أعاد ملخصاً فنياً، لكن جدول البيانات لم يظهر، مع أن الرد كان مقبولاً من حيث السعر وRSI والدعم والمقاومة.

الحكم: جزئي. يلزم توحيد إرجاع الجدول مع الرد النصي.

### CAED

- `Caed ؟` أعاد: لا يوجد سهم بهذا الاسم.
- `Caed ?` أعاد النتيجة نفسها.
- `ايوة عن سهم caed ؟` نجح وأعاد بيانات `CAED` كاملة.

الحكم: فشل وظيفي. الرمز اللاتيني القصير يجب أن يُفهم كسهم حتى بدون كلمة "سهم" أو سؤال إضافي.

## الأولويات المقترحة للإصلاح

1. منع خلط البيانات ذات التواريخ المختلفة في الرد، خصوصاً `CPME`.
2. تنفيذ شروط التجميع النصية كفلاتر حقيقية على `stock_scans_summary`.
3. تحسين استخراج الرموز اللاتينية المفردة مثل `CAED`, `CRST`, و`BIOC`.
4. جعل أسئلة "أشتري مين" تنتج ترتيباً واضحاً أو اختياراً مدعوماً بالأرقام المتاحة.
5. الحفاظ على مرجع السهم في المتابعات القصيرة مثل "مستهدفاته ايه".
6. عرض المتوسطات المتحركة فعلياً عندما يطلبها المستخدم، أو التصريح بأنها غير متاحة.

## نتيجة الإصلاح وإعادة التحقق

بعد إعادة الفرع إلى `61561c9`، تم إصلاح العيوب المثبتة التالية فقط:

- إضافة `CAED` إلى قائمة الرموز الصالحة، ودعم الرمز بحالة Title Case مثل `Caed ?`.
- توريث السهم الحالي لأسئلة الأهداف القصيرة مثل `مستهدفاته ايه`.
- تحويل شروط التجميع الرقمية إلى فلتر فعلي على المسح بدلاً من تركها لتفسير النموذج.
- منع وراثة سهم سابق عندما توجد شركة صريحة في السؤال، مع الإبقاء على وراثة السهم في صيغة المقارنة مثل `قارن ده مع AMER`.
- إضافة خرائط مستقلة لعبارتي `الاسكندرية` و`المطاحن`، وتصحيح قراءة حقول بيانات الأسهم في المقارنة.

إعادة التحقق عبر الحسابات الفعلية:

- سؤال `الاسكندرية والمطاحن والشمس - اشتري مين بكره؟` عاد بالرموز `ALCN`, `AFMC`, `ELSH` واختار `ALCN` كأفضل توازن فني وفق البيانات المتاحة.
- فلتر التجميع أعاد فقط `AJWA`, `CPME`, `ASPI`, `DGTZ`، وكلها تحقق: تجميع أكبر من 75، حجم أكبر من 1.5x، تصريف صفر، وتجميع يومين أو أكثر.
- `Bioc مشترية عند 383` ثم `مستهدفاته ايه` حافظ على `BIOC` في الجلسة.
- `Caed ?` عاد مباشرة ببيانات `CAED` وجدوله.
- الطلب المركب لـ `RAYA` يعرض الآن تحليل السهم والدعم/المقاومة، ثم يضيف قائمة أسهم التجميع في نفس الرد بدلاً من إسقاط الجزء الثاني.
- مسح التجميع العام يعيد الاتجاه الصحيح `accumulation` مع 16 سهماً مطابقاً، وأفضلهم: `AJWA`, `CPME`, `ASPI`, `DGTZ`, `AFMC`.
- سؤال `في أي منطقة` لـ `GDWA` يعيد تصنيف المنطقة مباشرة (`منطقة حيادية للمراقبة`) بدون مقالات إنشائية.

الاختبارات: `132/132` ناجحة في حزم اختبارات الشات الأساسية.
