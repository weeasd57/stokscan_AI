import DonateClient from "./DonateClient";

export const metadata = {
  title: "Support EGX Bots | Donate",
  description: "Support EGX Bots to keep the platform ad-free and help us cover server costs.",
};

export default function DonatePage() {
  return <DonateClient />;
}
