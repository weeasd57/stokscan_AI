# ملف ملاحظات إعادة هيكلة قلب الشات — 16 سبتمبر 2026

> حالة الكود: 28 ملفاً غير ملتزمة (uncommitted) على الـworktree المحلي.
> لم يتم أي commit أو deployment للإصلاحات الحالية — كل ما في الإنتاج ما زال السلوك القديم.
> هذا الملف توثيق فقط، بدون أي تعديل كود.

---

## أولاً: تصحيح وقائع مهم قبل القائمة

بعض نقاط المراجعة الخارجية تحتاج تدقيقاً لأن الواقع أخف أو أثقل:

| ادعاء المراجعة | الواقع المتحقق منه في الكود |
|---|---|
| «لا يوجد عقد أدلة إطلاقاً» | **غير دقيق جزئياً** — يوجد `Evidence` interface في `types.ts:132` مع `freshness: live/fresh/stale/unknown`، ودالة `attachEvidenceContract` في `pipeline.ts:172` ترفقه بكل نتيجة. لكن الأدوات نفسها لا تُنتجه أصلاً (يُركَّب لاحقاً في الـpipeline)، ولا توجد حالة `available/unsupported/failed` — الإصلاح نصف مكتمل |
| «الذاكرة لم تُصلح إطلاقاً» | **غير دقيق جزئياً** — أُصلحت في طبقة الحفظ: `persistPipelineSession` (pipeline.ts:2864-2866) يمسح `last_image_symbols` عند وجود رمز نصي أحدث أو مسح سياقي. لكن `resolveReference` في `memory.ts:32-38` ما زال يفضّل الصورة على `current_symbol` في ترتيب الحل نفسه — الإصلاح يعمل بالتعاون لكن منطق الأولوية لم يتغير |
| «اختبارات البث القديمة ما زالت موجودة» | صحيح — `stream-completion.test.js` ما زال يفحص نصوص الملفات لا السيناريو |

---

## ثانياً: الملاحظات المتبقية مرتبة بالأولوية

### أولاً حرجة (تحتاج إصلاحاً قبل النشر)

#### 1. صياغة الرفض الواثقة عند غياب البيانات اللحظية — من لوجات 15/9

**المشكلة الأوضح في اللوجات.** المستخدم قال «ازاي ده ب٥.٥» فقال النظام بثقة:

```text
السعر الحالي المسجل 5.00 جنيه (بيانات 2026-09-14)، مش 5.50
```

ثم بعد 6 ساعات نفس النظام قال «السهم يتداول عند 5.52 جنيه». المستخدم كان محقاً والنظام أنكر رقمه بتوكيد.

- **السبب:** المصدر كان `supabase` (إغلاق اليوم السابق) واللحظي غير متاح لحظتها، لكن الصياغة حولت «غير متوفر عندي» إلى «رقمك غلط».
- **الإصلاح المطلوب:** في تعليمات الـresponder ومنطق `buildDeterministicResponse`: عند غياب اللحظي، الصياغة الإلزامية تكون «آخر إغلاق مسجل عندي X بتاريخ Y، والسعر اللحظي غير متاح عندي حالياً» — لا يوجد أي إنكار لرقم المستخدم.
- **المكان:** `final-v2.ts` (تعليمات النظام) + أي مسار fallback يذكر سعراً مؤرشفاً.

#### 2. تغطية الأسعار اللحظية — رموز بلا mapping بديل

- لا يوجد mapping للرموز ولا مزود بديل عندما TradingView لا يجد الرمز (مثل `KASABF`).
- **الإصلاح المطلوب:** جدول alias للرموز + مصدر لحظي احتياطي (مثلاً EGX مباشر أو مصدر آخر) مع تمييز المصدر في Evidence.
- **المكان:** `live-stock-updater.ts` + طبقة رموز جديدة.

#### 3. الذاكرة — ترتيب حل «ده» ما زال صورة-أولاً

- `memory.ts:32-38`: وجود أي `last_image_symbols` يتقدم على `current_symbol` بثقة 0.9.
- الإصلاح الحالي (مسح المرجع عند رمز نصي) يخفف المشكلة لكن الترتيب نفسه خاطئ مفاهيمياً.
- **الإصلاح المطلوب:** مرجع زمني حقيقي — تخزين `last_reference_at` مع كل مرجع (نصي أو صوري) وحل «ده» لأحدث مرجع زمنياً بغض النظر عن نوعه، مع ترتيب الرسائل.

### ثانياً عالية (خطوة تالية مخططة)

#### 4. الفهم لا يزال deterministic-أولاً

- `pipeline.ts:2166`: المسار يبدأ من `buildCompoundDeterministicPlan` ثم تعديلات — لم يتحول لـLLM planner بخطة منظمة (JSON schema) كما في الخطة الأصلية.
- الهجين الحالي (deterministic + LLM review بعد الأدوات) أفضل من القديم لكن المرونة في الصياغات الجديدة محدودة بحدود الـregex.
- **ملاحظة من اللوجات تدعم ذلك:** «النهاردة ١٥\٩» رُفضت كرسالة غامضة بدل تفسيرها كطلب تحديث للسهم السياقي — وهذا فشل توجيه نمطي.

#### 5. عقد الأدلة نصف مكتمل

- Evidence يُركَّب في الـpipeline (pipeline.ts:2451) وليس ناتجاً أصلياً من الأدوات.
- لا توجد حالة توحّدية `available / stale / unsupported / failed` لكل أداة؛ الأدوات ما زالت ترجع أشكال بيانات مختلفة.
- **الإصلاح المطلوب:** كل أداة تُرجع `ToolResult` مع `evidence[]` من مصدرها + `availability` field، والـpipeline لا يركّبها اصطناعياً.

#### 6. الإلغاء غير شامل

- `live-stock-updater` (يُستدعى من `tools-v2.ts:5` و`portfolio-tools.ts:18`) له آلياته الخاصة ولا يرث `executionSignal` المركزي.
- مرحلة الصور (vision) لها controller خاص.
- النتيجة: طلب ملغى قد يترك تحديثات لحظية أو تحليل صور يعمل في الخلفية.

#### 7. الأخبار: لا يوجد تشغيل دوري بمراحل اعتماد

- الموجود: بحث عند الطلب + فصل `discovered_at/published_at` + كاش لا يخزن الفشل الشبكي (corporate-actions.ts).
- المفقود: جمع دوري في الخلفية، مرحلة تحقق منفصلة عن الاكتشاف، ودورة حياة حدث (مقترح → معتمد → منفذ → ملغى).
- في اللوجات ظهر خبر «توزيعات أرباح (marketmakerseg.com)» بعنوان غير مفهوم كعنوان خبر — جودة تصنيف العناوين تحتاج تحسيناً.

### ثالثاً متوسطة (تحسينات جودة)

#### 8. تذبذب «مفيش بيانات» مقابل بيانات كاملة

في جلسة AMER (لوجات 15/9): رد كامل بالبيانات الساعة 11:52، ثم «مفيش أي بيانات» الساعة 11:53، ثم رد كامل 13:05. السبب غالباً تذبذب في توفر اللحظي أو في توجيه الرسائل القصيرة («النهاردة ١٥\٩»). مرتبط بالنقطتين 1 و4.

#### 9. اختبارات سيناريوهات حية متعددة الرسائل

- `stream-completion.test.js` القديم ما زال يفحص وجود عبارات في الملفات.
- الإضافة الجيدة: `pipeline-core-parity.test.ts` + `execution.test.ts` + `amer-scenario.test.ts` (132 اختباراً ناجحاً).
- المفقود: سيناريوهات محادثة حقيقية متعددة الرسائل (تغيير سياق، تاريخ، انقطاع شبكة، بطء مزود) مع قياس: اكتمال الإجابة، دقة الأرقام، زمن أول محتوى.

#### 10. ترميز اللوجات العربية في الطباعة المحلية

البيانات في قاعدة البيانات سليمة UTF-8، لكن أي طباعة PowerShell مباشرة تظهر mojibake. يجب دائماً استخدام `-X utf8` أو كتابة ملفات وسيطة عند فحص اللوجات مستقبلاً (ملاحظة تشغيلية، ليست عيباً في المنتج).

---

## ثالثاً: ما تم إنجازه فعلاً ومُتحقق منه (للاتزان)

1. **توحيد المسارين:** `runPipeline` يجمع أحداث `runPipelineCore` نفسه المستخدم في البث — مثبت بـ`pipeline-core-parity.test.ts`.
2. **autoFixNumbers:** يربط الرقم بالسهم في نفس السطر — مثبت باختبارين في `validator.test.ts`.
3. **إيقاف قص «إدارة المخاطر»:** العبارة لم تعد ضمن عبارات الحذف.
4. **execution.ts:** نطاق تنفيذ مركزي مع `AbortSignal` يمر على planner/vision/final/hybrid review، مع تنظيف timers في `finally`.
5. **الأخبار:** فصل `discovered_at` عن `published_at`، `published_at_unknown`، كاش لا يخزن فشل الشبكة، والخبر القديم لا يمنع البحث عن أحدث.
6. **تعليمات ML:** لم تعد تستنتج «تجنب السهم» من score منخفض.
7. **الاختبارات:** 132/132 ناجحة عبر 7 suites + `tsc` + `build`.

---

## رابعاً: ترتيب التنفيذ المقترح للدفعة القادمة

1. **صياغة الرفض عند غياب اللحظي** (النقطة 1) — الأسرع والأعلى أثراً على تجربة المستخدم، ولا تحتاج بنية جديدة.
2. **ترتيب حل «ده» زمنياً** (النقطة 3) — تعديل محصور في `memory.ts` + حقل `last_reference_at`.
3. **availability في عقد الأدلة** (النقطة 5) — استكمال النصف الموجود.
4. **إلغاء live-stock-updater** (النقطة 6).
5. **mapping الرموز + مزود بديل** (النقطة 2) — يحتاج بحثاً في المصادر المتاحة.
6. **LLM planner بالخطة المنظمة** (النقطة 4) — الأكبر، يُنفذ تدريجياً بعد قياس أثر 1-5.
7. **النشر:** بعد 1-3 على الأقل، commit + push + قياس من Vercel Observability ولوجات الشات الجديدة.
